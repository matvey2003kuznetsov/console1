#include <iostream>
#include<cmath>
using namespace std;

int main()
{
    float n, f = 1;
    std::cout << "Chislo: ";
    std::cin >> n;
    for (int i = 1; i <= n; i++) {
        f *= i;
    }
    std::cout << "Fact = " << f << endl;
    
    return 0;
}
